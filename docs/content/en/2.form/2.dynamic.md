---
title: Dynamic
tag: RDynamic
description: Renders a schema. object and array recurse on their own; a field with a slot hands control back to you.
---

`RDynamic` walks a schema and resolves each `type` through the generated
`components-map`. `object` and `array` recurse on their own.

Only `useRForm`'s **Mode 1** feeds `RDynamic`: without a `schema` there is
nothing to render.

`RDynamic` is also what resolves the schema conditionals: see
[Conditionals](/en/form/condicionais).

## The basics

::demo{src="Dynamic/basico" title="one schema, one form" :form="false"}
::

## The schema is a value

Swapping which schema `RDynamic` receives already swaps the form — the two
buttons below do not swap the template.

::demo{src="Dynamic/troca" title="two schemas, the same RDynamic" :form="false"}
::

## slot instead of type

A schema field that declares `slot` instead of `type` hands control back to your
template, with `fieldName` and `rule` in the slot scope.

::demo{src="Dynamic/slot" title="a field you draw yourself" :form="false"}
::

## Props

::props-table{component="Dynamic" kind="root"}
::
