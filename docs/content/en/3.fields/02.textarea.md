---
title: Textarea
tag: RTextarea
description: The same set of utils as Text, plus rows — and the living test for v-mask, which maska's own directive would not cover.
---

`RTextarea` is `RText` over several lines: same props, same utils, same
resolution of `mask` and `rule`. What it adds is `rows`, the initial height.

## The basics

::demo{src="Textarea/basico" title="label, placeholder and description"}
::

## rows

::demo{src="Textarea/rows" title="initial height in lines"}
::

## length

The same `RUtilsLength` as `RText`.

::demo{src="Textarea/length" title="character counter"}
::

## mask on a textarea

This is the field that justifies the module's own directive. `v-maska` does

```js
const a = e instanceof HTMLInputElement ? e : e.querySelector("input");
if (a == null || a?.type === "file") return;
```

— and a `<textarea>` falls outside both branches, so it is never bound. maska's
`MaskInput`, however, only uses `value`, `selectionStart`, `setSelectionRange`,
`addEventListener` and `dispatchEvent`, all of which a textarea has: only the
guard excludes it. The module's `vMask` instantiates `MaskInput` directly on the
element, input **or** textarea.

::demo{src="Textarea/mask" title="preset and user mask"}
::

## States

::demo{src="Textarea/estados" title="required, loading and error"}
::

## rule

::demo{src="Textarea/rule" title="min over a string"}
`min` picks the schema from the value: with a string, it becomes
`z.string().min()`.
::

## Props

::props-table{component="Textarea" kind="field"}
::

## ui layers

::ui-tree{component="Textarea"}
::
