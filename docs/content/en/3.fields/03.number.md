---
title: Number
tag: RNumber
description: Numeric field with increment controls. The model is number | null.
---

`RNumber`'s model is `number | null` — and that type does not come from
`default`, as it does in the other fields: the component passes `D` as the third
parameter of `Element<typeof defaults, "number", number | null>`, overriding what
would come out on its own.

## The basics

The `default` the component declares is `0`, so the field is already in the
model.

::demo{src="Number/basico" title="basics"}
::

## min, max and step

The controls move by `step` and stop at the limits.

::demo{src="Number/min-max-step" title="min, max and step"}
::

## Slots

A good place for a unit.

::demo{src="Number/slots" title="leading and trailing"}
::

## default

::demo{src="Number/default" title="default"}
::

## rule

`min` and `max` pick the schema from the **value**, not from the field type:
with a number in the model they become `z.number().min()` / `.max()`.

::demo{src="Number/rule" title="min, max and both together"}
::

## States

::demo{src="Number/estados" title="loading and error"}
::

## ui

::demo{src="Number/ui" title="per-field override"}
`trailing` inherits a lower opacity without touching the rest of `defaults`.
::

## Props

::props-table{component="Number" kind="field"}
::

## ui layers

::ui-tree{component="Number"}
::
