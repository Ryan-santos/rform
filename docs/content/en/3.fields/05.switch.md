---
title: Switch
tag: RSwitch
description: Boolean. Here placeholder becomes the text next to the button, not a floating label.
---

`RSwitch` is the one exception to the role of `placeholder`: in the other
fields it is the floating label that rises when the field is filled; here it is
the text on the same line as the button.

## label and placeholder

::demo{src="Switch/label-placeholder" title="both, together and apart"}
`label` sits above, as in the other fields.
::

## icon

`true` uses the default icons (`check` when on, `loading` while loading). The
object swaps each state for an icon of yours.

::demo{src="Switch/icon" title="icon"}
::

## default

::demo{src="Switch/default" title="already true in the model"}
::

## States

::demo{src="Switch/estados" title="required, description and error"}
::

## Props

::props-table{component="Switch" kind="field"}
::

## ui layers

::ui-tree{component="Switch"}
::
