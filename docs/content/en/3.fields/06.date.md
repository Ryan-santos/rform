---
title: Date
tag: RDate
description: Typeable date field with a calendar in a popover. The model changes shape with the mode.
---

`RDate`'s model changes **shape** with `mode`: a string in `single`, a tuple in
`range`, an array in `multiple`. And it is always ISO — the display format is a
different thing, and comes from the translation pack.

::callout{type="info"}
`formatIso` and the ISO branch of `parseIncoming` are deliberately **not**
localized: ISO is the model's format and is language-independent. Localizing it
would make the stored value change with the language on screen. What does change
with the locale is only the typing mask and the display, derived from
`rform.formats.date` — switch the language in the footer and watch the field
rewrite itself without the model changing.
::

## mode single

::demo{src="Date/single" title="one date"}
You can type straight into the field; the mask handles the format.
::

## mode range

::demo{src="Date/range" title="two fields in the same wrapper"}
The model becomes `[start, end]` — the tuple exists before the second date does.
::

## mode multiple

::demo{src="Date/multiple" title="loose dates"}
::

## time

::demo{src="Date/time" title="adds a time to the value"}
It combines with any `mode`.
::

## default

::demo{src="Date/default" title="default"}
::

## disable

`DisableSpec` has four independent keys: `before`, `after`, `dates` and
`between`.

::demo{src="Date/disable-limites" title="before and after"}
::

::demo{src="Date/disable-datas" title="dates"}
A list of closed days, without touching the rest of the calendar.
::

::demo{src="Date/disable-between" title="between"}
Closes a stretch in the middle — a break, a blocked schedule, a deploy freeze.
::

## States

::demo{src="Date/estados" title="required, loading and error"}
::

## Props

::props-table{component="Date" kind="field"}
::

## ui layers

::ui-tree{component="Date"}
::
