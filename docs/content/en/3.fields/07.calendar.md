---
title: Calendar
tag: RCalendar
description: The same calendar RDate opens in a popover, now always visible.
---

`RCalendar` accepts the same `mode`, `time` and `disable` as `RDate` — the
difference is that it does not open in a popover, it is always on screen.

::callout{type="tip"}
`Calendar` exists as a **field** and as a **util** (`RUtilsCalendar`, the panel
`RDate` mounts inside the popover). They do not collide: the translation prefix
is per scope, so one lives under `rform.fields.calendar.*` and the other under
`rform.utils.calendar.*`.
::

## mode

::demo{src="Calendar/single" title="single (default)" ui="grid gap-4 md:grid-cols-2"}
::

::demo{src="Calendar/range" title="range" ui="grid gap-4 md:grid-cols-2"}
Click one date and then the other.
::

::demo{src="Calendar/multiple" title="multiple" ui="grid gap-4 md:grid-cols-2"}
Clicking again removes it.
::

## time

::demo{src="Calendar/time" title="time alongside the range" ui="grid gap-4 md:grid-cols-2"}
::

## disable

::demo{src="Calendar/disable" title="the same DisableSpec as RDate" ui="grid gap-4 md:grid-cols-2"}
::

## States

::demo{src="Calendar/estados" title="required and error" ui="grid gap-4 md:grid-cols-2"}
::

## Props

::props-table{component="Calendar" kind="field"}
::

## ui layers

::ui-tree{component="Calendar"}
::
