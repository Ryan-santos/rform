---
title: Color
tag: RColor
description: HSV picker in a popover, with a swatch and a hex field. The model is always the #rrggbb string.
---

## The basics

The `default` the component declares is `#000000`, so the field is born filled
in the model.

::demo{src="Color/basico" title="basics"}
::

::demo{src="Color/label" title="label and description"}
::

## default

::demo{src="Color/default" title="default"}
Both are in the panel before you even open the picker.
::

## States

::demo{src="Color/estados" title="required, loading and error"}
::

## rule

::demo{src="Color/rule" title="inline function"}
Refusing pure black.
::

::callout{type="tip"}
The `border-white` on the picker markers is literal **by design**: the marker has
to contrast with an arbitrary color, so it is not a theme token somebody forgot
to swap. `text-white` would be drift; `border-white` here is not.
::

## Props

::props-table{component="Color" kind="field"}
::

## ui layers

::ui-tree{component="Color"}
::
