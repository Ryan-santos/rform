---
title: Object
tag: RObject
description: Groups fields under a key. It provides its own model to the children, but does not override the form root.
---

The children's `name`s become keys inside `RObject`'s key. It provides its own
model to the children, but does **not** override the form root — a rule down
there still sees the whole form, not just the branch.

## The basics

::demo{src="Object/basico" title="basics"}
::

## label and required

::demo{src="Object/required" title="the group's required"}
`required` here belongs to the group, not to the children — it marks the block's
label.
::

## Object inside an object

::demo{src="Object/aninhado" title="two levels"}
The ids stack (`empresa.matriz.cidade`), and the model follows the same shape.
::

## A nested rule sees the whole form

The `form` of a validation comes from `defineFormRoot`, provided **only** by
`RForm` — `RArray` and `RObject` do not override it.

::demo{src="Object/form-root" title="comparing with a field outside"}
::

## default

::demo{src="Object/default" title="default"}
An object's `default` is **cloned** per instance: `useField` calls
`structuredClone` precisely so a child cannot write into the object the
component declared at module scope.
::

## Props

::props-table{component="Object" kind="field"}
::

## ui layers

::ui-tree{component="Object"}
::
