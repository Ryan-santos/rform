---
title: Dynamic
tag: RDynamic
description: Renderiza um schema. object e array recursam sozinhos; um campo com slot devolve o controle pra você.
---

`RDynamic` percorre um schema e resolve cada `type` pelo `components-map`
gerado. `object` e `array` recursam sozinhos.

Só o **Modo 1** do `useRForm` alimenta o `RDynamic`: sem `schema` não há o que
renderizar.

## O básico

::demo{src="Dynamic/basico" title="um schema, um formulário" :form="false"}
::

## O schema é um valor

Trocar qual schema o `RDynamic` recebe já troca o formulário — os dois botões
abaixo não trocam o template.

::demo{src="Dynamic/troca" title="dois schemas, o mesmo RDynamic" :form="false"}
::

## slot em vez de type

Um campo do schema que declara `slot` em vez de `type` devolve o controle para o
seu template, com `fieldName` e `rule` no escopo do slot.

::demo{src="Dynamic/slot" title="um campo que você desenha" :form="false"}
::

## Props

::props-table{component="Dynamic" kind="root"}
::
