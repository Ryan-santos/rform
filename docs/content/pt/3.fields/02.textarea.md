---
title: Textarea
tag: RTextarea
description: O mesmo conjunto de utils do Text, mais rows — e o teste vivo do v-mask, que a diretiva do maska não cobriria.
---

`RTextarea` é o `RText` de várias linhas: mesmas props, mesmos utils, mesma
resolução de `mask` e `rule`. O que ele acrescenta é `rows`, a altura inicial.

## O básico

::demo{src="Textarea/basico" title="label, placeholder e description"}
::

## rows

::demo{src="Textarea/rows" title="altura inicial em linhas"}
::

## length

O mesmo `RUtilsLength` do `RText`.

::demo{src="Textarea/length" title="contador de caracteres"}
::

## mask num textarea

Este é o campo que justifica a diretiva própria. A `v-maska` faz

```js
const a = e instanceof HTMLInputElement ? e : e.querySelector("input");
if (a == null || a?.type === "file") return;
```

— e um `<textarea>` cai fora dos dois ramos, então nunca é bindado. O
`MaskInput` do maska, porém, só usa `value`, `selectionStart`,
`setSelectionRange`, `addEventListener` e `dispatchEvent`, que um textarea tem:
só o guard exclui. O `vMask` do módulo instancia o `MaskInput` direto no
elemento, input **ou** textarea.

::demo{src="Textarea/mask" title="mask de preset e do usuário"}
::

## Estados

::demo{src="Textarea/estados" title="required, loading e error"}
::

## rule

::demo{src="Textarea/rule" title="min sobre string"}
`min` escolhe o schema pelo valor: com uma string, vira `z.string().min()`.
::

## Props

::props-table{component="Textarea" kind="field"}
::

## Camadas de ui

::ui-tree{component="Textarea"}
::
