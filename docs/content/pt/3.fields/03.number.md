---
title: Number
tag: RNumber
description: Campo numérico com controles de incremento. O model é number | null.
---

O model de `RNumber` é `number | null` — e esse tipo não sai do `default`, como
nos outros campos: o componente passa `D` como terceiro parâmetro de
`Element<typeof defaults, "number", number | null>`, sobrescrevendo o que sairia
sozinho.

## O básico

O `default` que o componente declara é `0`, então o campo já nasce no model.

::demo{src="Number/basico" title="básico"}
::

## min, max e step

Os controles andam de `step` em `step` e param nos limites.

::demo{src="Number/min-max-step" title="min, max e step"}
::

## Slots

Bom lugar para unidade.

::demo{src="Number/slots" title="leading e trailing"}
::

## default

::demo{src="Number/default" title="default"}
::

## rule

`min` e `max` escolhem o schema pelo **valor**, não pelo tipo do campo: com um
número no model viram `z.number().min()` / `.max()`.

::demo{src="Number/rule" title="min, max e os dois juntos"}
::

## Estados

::demo{src="Number/estados" title="loading e error"}
::

## ui

::demo{src="Number/ui" title="override por campo"}
O `trailing` herda opacidade menor sem tocar no resto do `defaults`.
::

## Props

::props-table{component="Number" kind="field"}
::

## Camadas de ui

::ui-tree{component="Number"}
::
