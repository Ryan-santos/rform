---
title: Select
tag: RSelect
description: Dropdown com options em três formatos, seleção múltipla e slot de item.
---

`RSelect` aceita `options` em três formatos e decide sozinho o que vira valor e
o que vira rótulo. Compare no painel de model: sem `modelFull` o model guarda só
o valor; com `modelFull` guarda o objeto inteiro.

## options

### Array primitivo

O valor e o rótulo são o próprio item.

::demo{src="Select/primitivo" title="array primitivo"}
::

### Objeto `{ chave: rótulo }`

A chave vira o valor; o valor do objeto vira o rótulo. O que vai pro model é a
chave.

::demo{src="Select/objeto" title="objeto"}
::

### Array de objetos

`keyValue` diz qual campo vira o valor, `keyLabel` qual vira o texto. Os dois
têm default no `defaults` do componente (`id` e `name`) — e ficam **fora** de
`defaults.text` justamente porque não são texto: se estivessem lá dentro, o
auto-prefixo os transformaria em `rform.fields.select.id`, e o Select passaria a
procurar `option["rform.fields.select.id"]`.

::demo{src="Select/key-value-label" title="keyValue e keyLabel"}
::

## modelFull

::demo{src="Select/model-full" title="só o valor, ou o objeto inteiro"}
::

## multiple

O model vira array. `multiple` é declarado como `Multiple & boolean` — a
interseção é o que força o compilador SFC a emitir `type: Boolean`; sem ela o
atributo sem valor chegaria como string vazia, que é *falsy*.

::demo{src="Select/multiple" title="multiple, com e sem slot"}
::

## Slot

O mesmo slot desenha o campo e a linha da lista; `list` diz onde você está.

::demo{src="Select/slot" title="campo e linha da lista"}
::

## search

::demo{src="Select/search" title="busca dentro do painel"}
::

## default

Com `modelFull` o `default` é o objeto; sem ele, o valor.

::demo{src="Select/default" title="default"}
::

## Estados

::demo{src="Select/estados" title="required, loading e error"}
::

## Props

::props-table{component="Select" kind="field"}
::

## Camadas de ui

::ui-tree{component="Select"}
::
