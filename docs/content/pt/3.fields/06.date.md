---
title: Date
tag: RDate
description: Campo de data digitável com calendário em popover. O model muda de forma conforme o mode.
---

O model de `RDate` muda de **forma** conforme o `mode`: string em `single`,
tupla em `range`, array em `multiple`. E é sempre ISO — o formato de exibição é
outra coisa, e vem do pack de tradução.

::callout{type="info"}
`formatIso` e o ramo ISO de `parseIncoming` **não** são localizados de
propósito: ISO é o formato do model e é independente de idioma. Localizá-lo
faria o valor gravado mudar com o idioma da tela. O que muda com o locale é só a
máscara de digitação e a exibição, derivadas de `rform.formats.date` — troque o
idioma no rodapé e veja o campo se reescrever sem o model mudar.
::

## mode single

::demo{src="Date/single" title="uma data"}
Dá para digitar direto no campo; a mask cuida do formato.
::

## mode range

::demo{src="Date/range" title="dois campos no mesmo wrapper"}
O model vira `[início, fim]` — a tupla existe antes mesmo da segunda data.
::

## mode multiple

::demo{src="Date/multiple" title="datas avulsas"}
::

## time

::demo{src="Date/time" title="acrescenta hora ao valor"}
Combina com qualquer `mode`.
::

## default

::demo{src="Date/default" title="default"}
::

## disable

A `DisableSpec` tem quatro chaves independentes: `before`, `after`, `dates` e
`between`.

::demo{src="Date/disable-limites" title="before e after"}
::

::demo{src="Date/disable-datas" title="dates"}
Lista de dias fechados, sem mexer no resto do calendário.
::

::demo{src="Date/disable-between" title="between"}
Fecha um trecho no meio — recesso, bloqueio de agenda, freeze de deploy.
::

## Estados

::demo{src="Date/estados" title="required, loading e error"}
::

## Props

::props-table{component="Date" kind="field"}
::

## Camadas de ui

::ui-tree{component="Date"}
::
