---
title: Calendar
tag: RCalendar
description: O mesmo calendário que o RDate abre no popover, agora sempre visível.
---

`RCalendar` aceita os mesmos `mode`, `time` e `disable` do `RDate` — a
diferença é que ele não abre em popover, está sempre na tela.

::callout{type="tip"}
`Calendar` existe como **campo** e como **util** (`RUtilsCalendar`, o painel que
o `RDate` monta dentro do popover). Não colidem: o prefixo de tradução é por
escopo, então um vive em `rform.fields.calendar.*` e o outro em
`rform.utils.calendar.*`.
::

## mode

::demo{src="Calendar/single" title="single (padrão)" ui="grid gap-4 md:grid-cols-2"}
::

::demo{src="Calendar/range" title="range" ui="grid gap-4 md:grid-cols-2"}
Clique numa data e depois na outra.
::

::demo{src="Calendar/multiple" title="multiple" ui="grid gap-4 md:grid-cols-2"}
Clicar de novo remove.
::

## time

::demo{src="Calendar/time" title="hora junto com o intervalo" ui="grid gap-4 md:grid-cols-2"}
::

## disable

::demo{src="Calendar/disable" title="a mesma DisableSpec do RDate" ui="grid gap-4 md:grid-cols-2"}
::

## Estados

::demo{src="Calendar/estados" title="required e error" ui="grid gap-4 md:grid-cols-2"}
::

## Props

::props-table{component="Calendar" kind="field"}
::

## Camadas de ui

::ui-tree{component="Calendar"}
::
