---
title: Pin
tag: RPin
description: Código de N caracteres, um por caixinha. O model é a string inteira, não um array.
---

Cola, backspace e navegação com setas funcionam como num campo só — porque o
model **é** um campo só: a string inteira, não um array de caracteres.

## O básico

::demo{src="Pin/basico" title="básico"}
Cole um código completo e repare que ele se distribui pelas caixas.
::

## length

::demo{src="Pin/length" title="length"}
::

## separator

::demo{src="Pin/separator" title="separator"}
Insere um traço a cada N caixas. Só visual — o model continua sendo a string
corrida.
::

## type

::demo{src="Pin/type" title="numeric e alphanumeric"}
`numeric` aceita só dígitos; `alphanumeric` aceita letras e mostra em maiúsculas.
::

::callout{type="warn"}
`type` fica **fora** de `defaults.text`, ao lado de `keyValue` e `keyLabel` do
Select. Dentro, o auto-prefixo o transformaria numa chave de tradução — e o
campo passaria a comparar `"numeric"` com `"rform.fields.pin.numeric"`.
::

## secret

::demo{src="Pin/secret" title="secret"}
Esconde os caracteres. O valor no painel continua legível — é o model, não a tela.
::

## onComplete

::demo{src="Pin/on-complete" title="onComplete"}
Dispara quando a última caixa é preenchida.
::

## Estados

::demo{src="Pin/estados" title="required, loading e error"}
::

## Props

::props-table{component="Pin" kind="field"}
::

## Camadas de ui

::ui-tree{component="Pin"}
::
