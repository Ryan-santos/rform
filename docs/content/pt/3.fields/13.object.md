---
title: Object
tag: RObject
description: Agrupa campos sob uma chave. Provê o próprio model para os filhos, mas não sobrescreve o form root.
---

Os `name` dos filhos viram chaves dentro da chave do `RObject`. Ele provê o
próprio model para os filhos, mas **não** sobrescreve o form root — uma rule lá
dentro continua enxergando o formulário inteiro, não só o galho.

## O básico

::demo{src="Object/basico" title="básico"}
::

## label e required

::demo{src="Object/required" title="required do grupo"}
O `required` aqui é do grupo, não dos filhos — marca o label do bloco.
::

## Objeto dentro de objeto

::demo{src="Object/aninhado" title="dois níveis"}
Os ids empilham (`empresa.matriz.cidade`), e o model acompanha a mesma forma.
::

## A rule aninhada enxerga o form inteiro

O `form` da validação vem de `defineFormRoot`, provido **só** pelo `RForm` —
`RArray` e `RObject` não o sobrescrevem.

::demo{src="Object/form-root" title="comparando com um campo de fora"}
::

## default

::demo{src="Object/default" title="default"}
O `default` de um objeto é **clonado** por instância: o `useField` faz
`structuredClone` justamente para um filho não escrever no objeto que o
componente declarou em escopo de módulo.
::

## Props

::props-table{component="Object" kind="field"}
::

## Camadas de ui

::ui-tree{component="Object"}
::
